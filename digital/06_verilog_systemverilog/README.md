# Verilog/SystemVerilog Basics

## 핵심 항목

- `wire`, `reg`, `logic`
- `assign`, `always_comb`, `always_ff`
- Blocking `=` vs non-blocking `<=`
- `parameter`, `generate`
- Testbench, clock/reset generation

## 예상 질문

- `always_comb`과 `always_ff`를 나눠 쓰는 이유는?
- 조합논리와 순차논리에서 assignment 스타일은 어떻게 다른가?
